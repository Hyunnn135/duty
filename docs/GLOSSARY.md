# 용어·개념 학습 노트

> 이 프로젝트를 진행하며 등장하는 전문 용어를 **비전공자도 복습할 수 있게** 쉬운 말로
> 정리합니다. 새 용어가 나올 때마다 계속 추가하는 살아있는 노트입니다.
> (각 항목: **한 줄 정의 → 쉬운 비유/설명 → 우리 프로젝트에서의 예**)

---

## 1. 근무표 생성 엔진 관련

### 솔버 (Solver)
- **정의**: 복잡한 조건(제약)을 모두 만족하는 답을 자동으로 찾아주는 프로그램.
- **비유**: 스도쿠를 대신 풀어주는 기계. 규칙만 알려주면 빈칸을 알아서 채움.
- **우리 예**: 간호사·날짜·근무 규칙을 넣으면 근무표를 채워주는 `scheduler.py`.

### 제약조건 (Constraint)
- **정의**: 답이 반드시(또는 되도록) 지켜야 하는 조건.
- **하드 제약(hard)**: **절대** 어기면 안 되는 것. (예: 나이트 다음날 데이 금지)
- **소프트 제약(soft)**: 되도록 지키되, 불가피하면 어길 수 있는 것. (예: 개인 오프 희망)
- **우리 예**: "교대별 최소 인원"=하드, "나이트 공평 분배"=소프트.

### 목적함수 (Objective function)
- **정의**: 여러 답 중 '더 좋은 답'을 고르는 **점수 기준**. 보통 벌점(페널티)을 최소화.
- **비유**: 시험 채점 기준표. 소프트 제약을 어길 때마다 감점 → 감점 총합이 가장 작은 답 선택.
- **우리 예**: (희망 미반영 × 10) + (나이트 편차 × 3) 를 최소화.

### OR-Tools / CP-SAT
- **정의**: 구글이 만든 **제약조건 최적화 라이브러리**(솔버 모음). CP-SAT는 그 안의 강력한 솔버.
- **비유**: 우리가 직접 스도쿠 푸는 알고리즘을 안 짜도 되게 해주는, 검증된 '풀이 엔진'.
- **우리 예**: 근무표의 모든 규칙을 CP-SAT에 넘겨 최적 근무표를 계산.

### 회전 매트릭스 (Rotation matrix)
- **정의**: '전날 근무 → 다음날 근무' 전이가 허용인지 금지인지 적은 표.
- **우리 예**: N(나이트) → D(데이) = 금지, D → E = 허용. (§6.2)

---

## 2. 검증·테스트 관련

### 솔버 검증 (Verification)
- **정의**: 우리가 만든 규칙/코드가 **실제와 맞는지 확인**하는 작업.
- **우리 예**: 실제 7·8월 근무표에 우리 규칙을 돌려, 위반이 몇 건 나오는지 세어봄
  (`data/verify_rules.py`). 위반 0건이면 그 규칙은 현실과 일치.

### 검산 / 체크섬 (Checksum)
- **정의**: 데이터가 맞는지 **다른 방법으로 한 번 더 대조**해 오류를 잡는 것.
- **비유**: 영수증 항목을 더한 값이 합계와 같은지 확인하는 것.
- **우리 예**: 근무표 셀을 하나씩 읽은 뒤, 각 줄의 D/E/N 개수를 세어 표에 인쇄된
  집계 숫자와 같은지 대조 → 전사 오류를 자동으로 잡음.

### 테스트 픽스처 (Test fixture)
- **정의**: 자동 테스트에 쓰려고 **미리 준비해 둔 고정 데이터**.
- **비유**: 요리 실습 전에 미리 계량해 둔 재료. 매번 똑같이 쓸 수 있음.
- **우리 예**: 실제 61병동 근무표(`schedule_2026_07.csv`)를 '정답지'로 삼아,
  코드를 고칠 때마다 규칙을 자동 검사.

### 슬라이딩 윈도우 (Sliding window) vs 달력주 (Calendar week)
- **슬라이딩 윈도우**: 7칸짜리 창틀을 1일씩 옮기며 매 구간을 검사(1-7, 2-8, 3-9…).
  "**어느 7일을 봐도**" 조건을 볼 때 씀.
- **달력주**: 달력에 그어진 주(월~일). 고정된 칸(6일까지가 1주, 7~13일이 2주…).
  "**그 주에**" 조건을 볼 때 씀.
- **왜 중요한가**: 같은 "주 오프 2번" 규칙도 어느 기준이냐에 따라 결과가 완전히 달라짐.
  실제 표에 둘 다 돌려보니 슬라이딩은 전부 위반(틀린 해석), 달력주는 약 76~80% 충족
  → **규칙의 올바른 기준은 달력주**임을 데이터로 확인. (이게 '솔버 검증'의 실제 효과)

---

## 3. 데이터·웹 관련

### CSV / JSON
- **CSV**: 쉼표로 칸을 나눈 표 형식 텍스트. 엑셀로 바로 열림. (표 데이터에 적합)
- **JSON**: 이름:값 구조로 계층 데이터를 담는 형식. (프로그램끼리 주고받기 좋음)
- **우리 예**: 근무표를 사람이 보기 좋은 CSV와, 프로그램이 쓰기 좋은 JSON 둘 다로 저장.

### API / FastAPI
- **API**: 프로그램끼리 요청·응답을 주고받는 **약속된 창구**.
- **FastAPI**: 파이썬으로 API 서버를 쉽게 만드는 도구.
- **우리 예**: 웹 화면이 "이 조건으로 근무표 만들어줘"를 API로 요청 → 서버가 결과 반환.

### Pydantic
- **정의**: 입력 데이터의 **형식·규칙을 자동 검사**해주는 파이썬 도구.
- **비유**: 서류 접수처에서 빈칸·형식 오류를 미리 걸러주는 직원.
- **우리 예**: "간호사 id 중복 금지", "일수는 1~62" 같은 검사를 자동 수행(`models.py`).

---

## 3-2. 배포·운영 관련

### 서버리스/엣지 vs 컨테이너
- **엣지/서버리스(예: Cloudflare Workers)**: 아주 가벼운 코드를 전 세계에 뿌려 빠르게 실행.
  대신 **무거운 네이티브 프로그램은 못 돌림**.
- **컨테이너(예: Cloud Run, Render)**: 우리 프로그램 전체를 상자에 담아 '진짜 서버'에서 실행.
- **우리 예**: OR-Tools는 무거운 C++ 라이브러리라 **엣지 불가 → 컨테이너 필수**.

### Scale-to-zero (스케일 투 제로)
- **정의**: 아무도 안 쓰면 서버를 **0개로 줄여** 요금이 안 나오게 하는 방식.
- **비유**: 손님 없으면 불 끄고, 손님 오면 켜는 가게. 켜는 데 잠깐(콜드 스타트) 걸림.
- **우리 예**: Cloud Run이 이 방식이라 병동에서 가끔 쓰면 사실상 무료.

### 콜드 스타트 (Cold start)
- **정의**: 잠자던 서버가 첫 요청에 깨어나느라 **처음 한 번 느린 것**.
- **우리 예**: 한동안 안 쓴 뒤 근무표 첫 생성이 몇 초 느릴 수 있음(이후엔 빠름).

### RBAC (역할 기반 접근 제어)
- **정의**: 사용자에게 **역할**을 주고, 역할마다 할 수 있는 일을 정하는 권한 관리 방식.
- **우리 예**: 마스터/관리자(파트장)/일반(부서원) 3역할로 기능 접근을 제한.

### 멀티테넌시 (Multi-tenancy) / 멀티 병동
- **정의**: 하나의 시스템을 **여러 조직(병동)이 나눠 쓰되, 서로 데이터가 섞이지 않게** 하는 설계.
- **비유**: 한 건물에 여러 사무실이 세 들어 살지만, 각자 열쇠로 자기 방만 여는 것.
- **우리 예**: 51·61·62… 병동이 한 앱을 쓰되, 파트장은 **자기 병동 근무표만** 봄.
  이를 위해 모든 데이터에 '어느 병동(ward_id)' 표시 + RLS로 접근 격리.
- **왜 지금 설계?**: 나중에 넣으면 데이터 구조를 뜯어고쳐야 해서 아픔. 처음부터
  "병동 여러 개"를 전제로 만들면 병동 추가 = 행 하나 추가로 끝.

### Supabase / Postgres / 인증(Auth)
- **Postgres**: 널리 쓰는 무료 데이터베이스(표 형태로 데이터 저장).
- **인증(Auth)**: 회원가입·로그인으로 '누구인지' 확인하는 기능.
- **Supabase**: Postgres + 인증 + 권한을 **무료 티어로 한 번에** 주는 서비스.
- **우리 예**: 사용자·근무표·오프신청을 Supabase에 저장하고, 로그인/권한도 Supabase로 처리.

---

## 4. 근무표 도메인 용어 (병동 실무)

- **D / E / N / M**: 데이(07-15) / 이브닝(15-22) / 나이트(22-07) / 미드.
- **OFF**: 오프(휴무). **HY**: 연차. **경조**: 경조사 휴가. **S/S1**: 파트장 상근.
- **나이트 keep**: 나이트 후 오프를 붙여 회복을 보장하는 관행.
- **역회전**: E→D, N→D 처럼 휴식이 짧아지는 나쁜 근무 전환.
- **풍당풍당**: 근무가 하루씩 흩어져(고립) 배치되는 것. 지양 대상.
- **듀티(duty)**: 근무(표). '듀티표' = 근무표.

---

## 변경 이력
| 날짜 | 추가된 용어 |
|------|-------------|
| 2026-07-21 | 솔버, 제약조건, 목적함수, OR-Tools/CP-SAT, 회전 매트릭스, 솔버 검증, 검산/체크섬, 테스트 픽스처, 슬라이딩 윈도우, CSV/JSON, API/FastAPI, Pydantic, 근무 도메인 용어 |
| 2026-07-21 | (배포/운영) 서버리스/엣지 vs 컨테이너, scale-to-zero, 콜드 스타트, RBAC, Supabase/Postgres/인증, 달력주 |
