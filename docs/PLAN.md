# 간호사 근무표 자동 생성 — 설계 계획서

> **문서 성격**: 이 문서는 살아있는 설계 문서(living document)입니다.
> 구현 전 대원칙·병동 상황·근무표 이론을 확정하고, 개발이 진행되며 계속 갱신합니다.
> 상태 표기 — ✅ 확정 · 🟡 논의중 · ⬜ 미정(작성 예정)

- 최종 갱신: 2026-07-21
- 대상 저장소: `Hyunnn135/duty`
- 개발 브랜치: `claude/nurse-schedule-auto-generation-l6keyk`

---

## 0. 대원칙 (불가침 원칙) — ⬜ 작성 예정

> **가장 먼저 확정할 영역.** 어떤 최적화·편의보다 우선하며, 절대 위반되어서는 안 되는
> 규칙을 여기에 명시합니다. 솔버에서 **하드 제약**으로만 구현하고, 목적함수(소프트)로
> 절대 내려보내지 않습니다. 사용자가 항목을 전달하면 아래에 번호로 고정합니다.

| # | 대원칙 | 근거/비고 | 상태 |
|---|--------|-----------|------|
| P1 | *(작성 예정)* | | ⬜ |
| P2 | *(작성 예정)* | | ⬜ |

**대원칙 후보(예시 — 확정 아님):**
- 나이트 근무 다음 날은 D/E/M 배정 금지 (나이트 후 최소 휴식)
- 신규·임신 간호사 나이트 절대 배정 금지
- 승인된 연차·공가 등 사전 휴무는 절대 변경 불가
- 교대별 최소 인원 미달 절대 불가 (환자 안전)
- 근무 간 최소 연속 휴식 시간(법정) 준수

---

## 1. 프로젝트 개요

3교대(+미드) 간호사 근무표(듀티표)를 제약조건 최적화로 자동 생성하는 웹 애플리케이션.
Google OR-Tools **CP-SAT** 솔버로 병동 고유 규칙을 만족하는 월간 근무표를 계산한다.

### 확정된 기본 요구사항 (요구사항 인터뷰 결과) — ✅

| 항목 | 내용 |
|------|------|
| 애플리케이션 형태 | 웹 앱 (우선 웹 화면 조회) |
| 기술 스택 | Python (FastAPI + OR-Tools + Pydantic) |
| 교대 구조 | 3교대(D/E/N) **+ 미드(M)** |
| 비근무 코드 | 연차·공가·병가·Edu 등 표에 코드로 표시 |
| 병동 규모 | 약 16–25명 |
| 편성 주기 | 월 단위 (약 28–31일) |
| 회전 규칙 | 역회전(E→D, N→D 등) 금지, 순방향(D→M→E→N) 선호 |
| 간호사 속성 | 신규·임신 간호사 나이트 제외 |
| 월 운영 | 근무일/오프/나이트 균등, 사전 휴무 반영, 전월 말일 이어받기, 개인 오프/나이트 신청 |

---

## 2. 근무 코드 체계 — ✅(초안)

| 구분 | 코드 | 의미 | 스케줄링 취급 |
|------|------|------|----------------|
| 근무 | `D` | 데이 | 인원 계산 · 회전 대상 |
| 근무 | `M` | 미드 | 인원 계산 · 회전 대상 |
| 근무 | `E` | 이브닝 | 인원 계산 · 회전 대상 |
| 근무 | `N` | 나이트 | 인원 계산 · 회전 대상 |
| 휴무 | `O` | 오프 | 근무 아님(연속근무 리셋) |
| 비근무 | `연차`/`공가`/`병가`/`Edu` 등 | 사전 확정 휴무·비근무 | 오프처럼 취급 + 표에 코드 표시 |

> 미드(M) 근무 시간대·역할은 병동 상황(3장)에서 구체화.

---

## 3. 병동 상황 & 기능 범위 — ⬜ 작성 예정

> 사용자가 전달하는 실제 병동 운영 정보를 여기 축적하고, 그에 따라 **넣을 기능 / 뺄 기능**을
> 결정한다.

### 3.1 병동 운영 정보 (사용자 입력 축적)

**대상 병동: 61병동 (샘플)**
- 병실 약 15개: **601~614호 + 616호** (615호 없음)
- 병실을 **3개 팀**으로 분리 운영 (팀별 담당 병실 그룹)
- **팀 연속성 원칙**: 간호사는 가능하면 **한 팀 내에서 계속 근무** → 환자 파악에 유리.
  불가피한 경우에만 팀 변경 허용 → **소프트 제약**(가능하면 유지, 강제는 아님)

**팀 구성 (확정)**
- **3개 팀**, **팀당 약 7~8명** (전체 약 21~24명 규모)
- **교대별 팀별 최소 인원 필요** ✅ — 최소 인원 제약이 **전체가 아니라 팀 단위**.
  즉 각 교대(D/E/N/M)에 팀1·팀2·팀3 각각 최소 인원이 채워져야 함.

**아직 확정 안 된 값** *(작성 예정)*
- **팀↔근무표 연동 방식**: 팀이 간호사 **고정 속성**인지, 근무표에서 **배정**하는지 ⬜(추후 확정)
- 팀별 담당 병실 구성(어느 호실이 어느 팀인지)
- 미드(M) 시간대·인원, 신규 기준 개월 수, 개인 신청 월 최대 개수, 공휴일 처리, 주말 정책 등

### 3.1.1 팀(Team) 개념 — 🟡 모델링 방향 확인 중
- Nurse에 `team` 속성 추가 (소속 팀)
- **교대별 팀별 최소 인원** = 하드 제약 (팀 단위 커버리지 보장)
- 팀 연속성 = **소프트 제약**: 같은 간호사가 기간 내 팀을 자주 바꾸지 않도록 감점
- ⬜ **미결정 — 이 결정이 모델 구조를 좌우함**:
  - (A) 팀이 **고정 속성** → 각 팀 소속 간호사 부분집합에 대해 팀별 최소 인원 제약을 검
  - (B) 팀도 **근무표에서 배정** → 팀 배정 변수 추가(간호사×일×팀), 연속성 소프트 제약
  - 사용자 확정 후 반영 (팀당 7~8명이면 (A) 고정이 자연스러우나 확인 필요)

### 3.2 포함 기능 (In scope)
- *(작성 예정)*

### 3.3 제외 기능 (Out of scope)
- *(작성 예정)*

---

## 4. 근무표 작성 이론 노트 — 🟡

> 근무표 작성의 기본 이론·관행을 정리하고, 각 이론이 어떤 제약조건/목적함수로
> 매핑되는지 연결한다.
> 출처: 간호관리학 실습 자료(네이버 블로그 ooxzx/223568435513, 화면 캡처로 전달) +
> 표준 듀티 이론(다우오피스 HR 블로그, Haruduty/DutyFlow 가이드) + 유튜브
> "간호사 쓰레기 근무표 경연대회 2탄"(잘못된 근무표 예시).

### 4.1 근무 시간대 (참고 자료 기준, 미드 없는 순수 3교대)
- D 07:00–15:00 · E 15:00–22:00 · N 22:00–07:00
- ※ 우리 병동은 여기에 **미드(M)** 추가 → 시간대·인원은 3장(병동 상황)에서 확정.

### 4.2 근무표 작성 규칙 (이론 → 구현 매핑)

| # | 이론/관행 | 설명 | 매핑되는 구현 | 하드/소프트 | 상태 |
|---|-----------|------|----------------|-------------|------|
| T1 | 5연속 근무 → 2 OFF | 5일 연속 근무 시 오프 2개 부여 | 5일 근무 창 뒤 OFF 2개 강제 | 하드 | 🟡 |
| T2 | 주 오프 ≥ 2 | 한 주에 오프 2번 선호 | **달력주(월~일) 오프+연차 ≥2**(소프트) | **소프트** | ✅ 재정의(§4.4) |
| T3 | 주 근무 ≤ 6 | 주 6일 이상 근무 금지 | 7일 슬라이딩 윈도우 근무 ≤ 6 (= 7일마다 최소 1 휴무) | 하드 | ✅ 검증됨 |
| T4 | 월 OFF 쿼터 | 그 달 토(SO)+일(주OFF)+공휴일(HO) 갯수만큼 오프 반영 | 간호사별 목표 OFF 수 = 주말+공휴일 수 | 하드/소프트(결정) | ⬜ |
| T5 | OFF 라벨 구분 | 토=SO, 일=주OFF, 공휴일=HO (표시용) | 표시 코드 세분화(연차와 별개) | 표시 | 🟡 |
| T6 | 근무 블록 ≥ 2 (풍당풍당 X) | N 포함 모든 근무는 연속 2개 이상, 고립 단일근무 금지 | 같은 근무 고립 배정 금지(블록 길이 ≥ 2) | 하드(또는 강한 소프트) | ⬜ |
| T7 | 나이트 후 OFF | 마지막 나이트 다음 날 반드시 OFF | N 블록은 반드시 OFF로 종료 | 하드 | ✅ |
| T8 | 연속 나이트 상한 | 나이트 4번 이후 OFF 필수(자료 기준 최대 4) | 연속 N ≤ 상한(병동값 확정 필요, 초안 3) | 하드 | ⚠️ 값 확인 |
| T9 | 월 나이트 상한 | 한 달 N ≤ 7, 2개월 합 ≤ 15 (병원마다 상이) | 간호사별 월/2개월 N 합 상한 | 하드(파라미터) | ⚠️ 값 확인 |
| T10 | N전담(N KEEP) | 야간 전담은 월 N 15개, 나이트 연속 2~3개로 | 전담 간호사 전용 규칙 세트 | 하드(파라미터) | ⚠️ 도입 여부 |
| T11a | 지양 패턴(하드) | E→D 직접, N-OFF-D(NOD) | 전이/2일갭 금지 | **하드** | ✅ 검증(0건) |
| T11b | 지양 패턴(소프트) | E-OFF-D(EOD) | 감점(지양하되 허용) | **소프트** | ✅ 재정의(§4.4) |
| T12 | 임산부·산부 보호 | 출산·유산·사산 후 1년 이내 야간·연장근로 불가 | 나이트 제외 대상에 포함(법규) | 하드(법규) | ✅ |
| T13 | 경력 분포 | 듀티마다 경력자 고루, 특히 N에 경력자+신규 혼합 | 교대별 경력자 ≥ 1 등 | 소프트→가능시 하드 | ⬜ |

### 4.4 실제 근무표 검증 결과 (2026 7·8월 대조) — ✅ 데이터 근거 확보

`data/verify_rules.py` 로 실제 근무표에 규칙을 대조한 결과. **현실이 채점한 규칙**이다.

**✅ 100% 지켜짐 → 하드 제약 확정**
| 규칙 | 7월 | 8월 |
|------|-----|-----|
| 나이트 후 D/E/M 금지(T7) | 0건 | 0건 |
| 역회전 E→D 금지 | 0건 | 0건 |
| N-OFF-D 금지(T11) | 0건 | 0건 |
| 주 근무 ≤ 6일(T3) | 0건 | 0건 |
| 연속 근무 ≤ 5일 | 최대 5 | 최대 5 |
| 연속 나이트 ≤ 3(T8) | 최대 3 | 최대 3 |

> **T8 확정**: 자료(블로그)는 "나이트 4회 후 오프"였으나 실제 표는 **연속 나이트 최대 3**.
> → 우리 병동 기준 **연속 나이트 상한 = 3**.

**⚠️ 예외 존재 → 소프트/재검토**
| 규칙 | 7월 | 8월 | 조치 |
|------|-----|-----|------|
| EOD(E-OFF-D) 지양(T11) | 20건 | 14건 | 하드 아님 → 약한 소프트 |
| 고립근무(풍당풍당, T6) | 4건 | 3건 | 강한 소프트(드문 예외 허용) |
| 주 OFF ≥ 2(T2) | 141건 | 70건 | (아래에서 재정의됨) |

**T2 "주 오프 ≥ 2" 재정의** (여러 해석을 실제 표에 대조한 실험 결과):
- 슬라이딩 7일·OFF만 = 211/211 위반(전부) → 완전히 틀린 해석
- 달력주(월~일)·오프+연차 = 68주 중 16주만 <2 (**충족률 약 76~80%**)
- **어떤 해석으로도 위반 0건이 안 됨** → **하드 규칙이 아님**. 실제 표는 나이트 몰린 주에
  오프 1개인 주를 허용(예: 조하은 7월 6~12일 = 6일 근무+오프 1).
- **결론**:
  - 하드(항상 지켜짐): **7일마다 최소 1 휴무**(= 주 근무 ≤6, T3).
  - 소프트(선호): **달력주(월~일) 오프+연차 ≥2** 되도록 목적함수 감점.
  - 근거: 블로그의 "토=SO + 일=주OFF"가 곧 한 주의 오프 2개(주말) → 달력주가 기준.

**나이트 공정성**: 1인 평균 약 5.7~5.8개, 최소~최대 5~7개(전출자 제외) — 실제로 고르게 분배됨.

**T11 지양 패턴 세분화** (E-OFF-D 재검토 결과):
- 블로그가 "지양"으로 묶은 3개 중 실제로는 **하드 2 + 소프트 1**로 갈림.
  - **E→D 직접 = 0건, N-OFF-D = 0건** → **하드**(절대 안 함).
  - **E-OFF-D(EOD) = 20건/14건** → **소프트**(지양하되 불가피 시 허용).
- EOD 실제 빈도: 1인당 월 **0.6~0.8건**, 간호사 절반가량은 **0건**, 전체 오프의 **6~10%**.
- 이유: N-OFF-D는 나이트 후 오프 1개뿐이라 회복 부족(하드), E-OFF-D는 휴식 약 33h로
  덜 심각(소프트).
- **구현**: EOD는 **중간 세기 소프트 감점** — 목표는 실제 표 수준(1인 월 ≤1건)으로 희소하게 유지.

### 4.3 작성 순서 힌트 (사람이 짜는 순서 = 알고리즘 검증용)
1. **N을 규칙성 있게 2개씩 먼저** 배치 (→ 절반 완성)
2. N 뒤 OFF 부여
3. 공휴일·토·일 갯수로 필요한 OFF 수 계산
4. 요청사항(오프날·연차) 최대 반영
5. D·E 채우기

> CP-SAT는 전역 최적화라 이 순서를 그대로 따르진 않지만, 결과 검증·휴리스틱
> 초기해(warm start) 설계에 참고한다. "N이 근무표의 뼈대"라는 우리 접근과 일치.

---

## 5. 데이터 모델 (확장 방향) — 🟡

### 5.1 Nurse (간호사)
- `id`, `name`
- `night_eligible: bool` — 신규·임신 시 false (나이트 제외)
- `team` — 소속 팀(예: 1/2/3). 팀 연속성 소프트 제약에 사용
- (추후) `experience_level`, `role`(charge 등), `employment_type`(파트타임 등)

### 5.2 WardConfig (병동 설정)
- 요일별 **교대별·팀별** 최소 인원: 평일 / 주말 / 공휴일 구분
- 회전 규칙 매트릭스(6절 참고)
- 연속 근무일 상한 / 연속 나이트 상한
- 간호사별 최소 오프
- 팀 목록(3개) 및 팀당 인원(약 7~8명)

### 5.3 MonthContext (월 컨텍스트)
- `year`, `month` → 일수·요일·주말·공휴일 자동 산출
- 공휴일 소스: 수동 입력 vs 자동 (⬜ 결정 대기)

### 5.4 Carry-over (전월 이월)
- 간호사별 전월 말일 마지막 근무 시퀀스(예: 마지막 1–3일)
- 용도: 월초 연속근무·연속나이트·경계 회전 제약 계산

### 5.5 PreAssigned (사전 배정)
- `{nurse_id, day, code}` — 연차·공가 등 하드 고정

### 5.6 Requests (개인 신청)
- `{nurse_id, day, type: off|night, priority}` — 소프트 반영
- 월 최대 신청 개수 제한 (⬜ 결정 대기)

---

## 6. 제약조건 상세 — 🟡

### 6.1 하드 제약 (반드시 준수)
1. 각 간호사는 하루에 근무/휴무 중 정확히 하나 배정
2. 요일별·교대별·**팀별** 최소 인원 충족 (각 교대에 팀1/2/3 각각 최소 인원)
3. **역회전 금지 매트릭스** (전날→다음날 전이 금지)
4. 연속 근무일 상한 (전월 이월 포함)
5. 연속 나이트 상한 (전월 이월 포함)
6. 신규·임신 간호사 나이트 배정 금지
7. 사전 휴무(연차 등) 고정
8. 간호사별 최소 오프
9. *(대원칙에서 추가되는 항목 반영)*

### 6.2 회전 매트릭스 (초안)
근무 순서 랭크: `D(1) → M(2) → E(3) → N(4)`. 연속 근무일에서 랭크가 **감소**하는 전이 =
역회전 → 금지 또는 감점. 나이트 이후는 오프/나이트만 허용.

| 전날↓ \ 다음날→ | D | M | E | N | O |
|---|---|---|---|---|---|
| **D** | 허용 | 허용 | 허용 | 허용 | 허용 |
| **M** | 금지 | 허용 | 허용 | 허용 | 허용 |
| **E** | 금지 | 금지 | 허용 | 허용 | 허용 |
| **N** | 금지 | 금지 | 금지 | 허용 | 허용 |
| **O** | 허용 | 허용 | 허용 | 허용 | 허용 |

> 위 "금지/허용" 경계는 병동 상황·대원칙에 맞춰 조정(일부는 금지 대신 감점 가능).

### 6.3 소프트 제약 (목적함수로 최적화)
- 개인 오프/나이트 신청 반영률 최대화
- 근무일·오프·나이트·**주말 오프** 균등 분배(편차 최소화)
- 순방향 회전 선호 / 비선호 전이 감점

---

## 7. 아키텍처

현재 스택 유지 · 모듈 정리:

```
duty/
├── app/
│   ├── models.py       # 데이터 모델 (확장)
│   ├── config.py       # 기본값·회전 매트릭스 (신규)
│   ├── scheduler.py    # CP-SAT 솔버 (제약별 정리)
│   ├── main.py         # FastAPI
│   └── static/index.html
├── tests/
├── docs/PLAN.md        # 본 문서
└── requirements.txt
```

---

## 8. 단계별 로드맵

| 단계 | 내용 | 산출물 | 상태 |
|------|------|--------|------|
| Phase 0 | 뼈대(현재): 기본 3교대 솔버·API·UI·테스트 | 완료 | ✅ |
| Phase 1 | 데이터 모델 확장 (미드·비근무코드·간호사속성·월컨텍스트·이월·사전배정·신청) | `models.py` + 테스트 | ⬜ |
| Phase 2 | 솔버 고도화 (미드·요일별인원·회전매트릭스·나이트제외·이월계산·공정성 확장) | `scheduler.py` + 테스트 | ⬜ |
| Phase 3 | 웹 UI 고도화 (속성 편집·달력형 그리드·신청/휴무 입력·통계 패널) | 프론트 | ⬜ |
| Phase 4 | (후순위) 수동수정 후 재최적화·프리셋 저장/불러오기·엑셀 내보내기·위반 리포트 | 운영 편의 | ⬜ |

---

## 9. 결정 대기 항목 (Open Questions)

- [ ] 대원칙(0절) 확정
- [ ] **팀↔근무표 연동**: 팀이 고정 속성(A)인지 근무표에서 배정(B)인지 (§3.1.1)
- [ ] 팀별 담당 병실 구성, 교대별 팀별 최소 인원 수치
- [ ] 미드(M) 근무 시간대·최소 인원, 미드도 블록 ≥2·회전에 포함하는지
- [x] **연속 나이트 상한 = 3** (실제 표 검증으로 확정, §4.4)
- [ ] **월 나이트 상한**: 월 7 / 2개월 15 가 우리 병동에도 맞는지 (T9)
- [ ] **N전담(N KEEP) 도입 여부** 및 규칙 (월 15개 등) (T10)
- [ ] **월 OFF 쿼터**를 "주말+공휴일 수"로 하드 고정할지 목표(소프트)로 둘지 (T4)
- [ ] 근무 블록 ≥2(풍당풍당 금지)를 하드로 둘지 강한 소프트로 둘지 (T6)
- [ ] 신규 기준(입사 몇 개월까지 나이트 제외)
- [ ] 개인 신청 월 최대 개수·우선순위 규칙
- [ ] 공휴일 입력 방식(수동 vs 자동)
- [x] 회전 매트릭스 "금지 vs 감점" 경계 일부 확정: E→D·N-OFF-D=금지(하드), E-OFF-D=감점(소프트)
- [ ] 주말 오프 공정성 정책

---

## 10. 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-07-21 | 최초 작성. 요구사항 인터뷰 결과·로드맵·제약조건 초안 정리. 대원칙/병동상황/이론 섹션 자리 확보. |
| 2026-07-21 | 4장 이론 노트 정리(간호관리학 실습 자료 기반 T1~T13 규칙·구현 매핑, 작성 순서 힌트). 9장에 병동별 확인 항목 추가. |
| 2026-07-21 | 3장 병동 상황 기록: 61병동(601~614·616호, 약 15병실), 3개 팀 운영, 팀 연속성 소프트 제약. Nurse에 team 속성 추가. |
| 2026-07-21 | 팀 세부 확정: 팀당 7~8명, 교대별 팀별 최소 인원(하드). 팀 고정 속성 vs 배정은 미결(모델 구조 좌우). |
| 2026-07-21 | 솔버 검증 시행(data/verify_rules.py): 실제 7·8월 표로 규칙 대조. 4.4절 추가. 연속 나이트 상한=3 확정. docs/GLOSSARY.md(용어 학습 노트) 신설. |
| 2026-07-21 | 주 오프≥2 재정의: 하드=7일마다 최소1휴무, 소프트=달력주 오프+연차≥2. |
| 2026-07-21 | EOD 재검토: 지양 패턴을 하드(E→D·N-OFF-D)/소프트(E-OFF-D)로 세분화(T11a/T11b). |
