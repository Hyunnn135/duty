# 간호사 근무표 자동 생성 — 설계 계획서

> **문서 성격**: 이 문서는 살아있는 설계 문서(living document)입니다.
> 구현 전 대원칙·병동 상황·근무표 이론을 확정하고, 개발이 진행되며 계속 갱신합니다.
> 상태 표기 — ✅ 확정 · 🟡 논의중 · ⬜ 미정(작성 예정)

- 최종 갱신: 2026-07-21
- 대상 저장소: `Hyunnn135/duty`
- 개발 브랜치: `claude/nurse-schedule-auto-generation-l6keyk`

---

## 0. 대원칙 (불가침 원칙) — ⬜ 작성 예정

> **가장 먼저 확정할 영역.** 어떤 최적화·편의보다 우선하며, 절대 위반되어서는 안 되는
> 규칙을 여기에 명시합니다. 솔버에서 **하드 제약**으로만 구현하고, 목적함수(소프트)로
> 절대 내려보내지 않습니다. 사용자가 항목을 전달하면 아래에 번호로 고정합니다.

| # | 대원칙 | 근거/비고 | 상태 |
|---|--------|-----------|------|
| P1 | *(작성 예정)* | | ⬜ |
| P2 | *(작성 예정)* | | ⬜ |

**대원칙 후보(예시 — 확정 아님):**
- 나이트 근무 다음 날은 D/E/M 배정 금지 (나이트 후 최소 휴식)
- 신규·임신 간호사 나이트 절대 배정 금지
- 승인된 연차·공가 등 사전 휴무는 절대 변경 불가
- 교대별 최소 인원 미달 절대 불가 (환자 안전)
- 근무 간 최소 연속 휴식 시간(법정) 준수

---

## 1. 프로젝트 개요

3교대(+미드) 간호사 근무표(듀티표)를 제약조건 최적화로 자동 생성하는 웹 애플리케이션.
Google OR-Tools **CP-SAT** 솔버로 병동 고유 규칙을 만족하는 월간 근무표를 계산한다.

### 확정된 기본 요구사항 (요구사항 인터뷰 결과) — ✅

| 항목 | 내용 |
|------|------|
| 애플리케이션 형태 | 웹 앱 (우선 웹 화면 조회) |
| 기술 스택 | Python (FastAPI + OR-Tools + Pydantic) |
| 교대 구조 | 3교대(D/E/N) **+ 미드(M)** |
| 비근무 코드 | 연차·공가·병가·Edu 등 표에 코드로 표시 |
| 병동 규모 | 약 16–25명 |
| 편성 주기 | 월 단위 (약 28–31일) |
| 회전 규칙 | 역회전(E→D, N→D 등) 금지, 순방향(D→M→E→N) 선호 |
| 간호사 속성 | 신규·임신 간호사 나이트 제외 |
| 월 운영 | 근무일/오프/나이트 균등, 사전 휴무 반영, 전월 말일 이어받기, 개인 오프/나이트 신청 |

---

## 2. 근무 코드 체계 — ✅(초안)

| 구분 | 코드 | 의미 | 스케줄링 취급 |
|------|------|------|----------------|
| 근무 | `D` | 데이 | 인원 계산 · 회전 대상 |
| 근무 | `M` | 미드 | 인원 계산 · 회전 대상 |
| 근무 | `E` | 이브닝 | 인원 계산 · 회전 대상 |
| 근무 | `N` | 나이트 | 인원 계산 · 회전 대상 |
| 휴무 | `O` | 오프 | 근무 아님(연속근무 리셋) |
| 비근무 | `연차`/`공가`/`병가`/`Edu` 등 | 사전 확정 휴무·비근무 | 오프처럼 취급 + 표에 코드 표시 |

> 미드(M) 근무 시간대·역할은 병동 상황(3장)에서 구체화.

---

## 3. 병동 상황 & 기능 범위 — ⬜ 작성 예정

> 사용자가 전달하는 실제 병동 운영 정보를 여기 축적하고, 그에 따라 **넣을 기능 / 뺄 기능**을
> 결정한다.

### 3.1 병동 운영 정보 (사용자 입력 축적)
- *(작성 예정: 미드 시간대·인원, 신규 기준 개월 수, 개인 신청 월 최대 개수, 공휴일 처리, 주말 정책 등)*

### 3.2 포함 기능 (In scope)
- *(작성 예정)*

### 3.3 제외 기능 (Out of scope)
- *(작성 예정)*

---

## 4. 근무표 작성 이론 노트 — ⬜ 작성 예정

> 사용자가 알려주는 근무표 작성의 기본 이론·관행을 여기 정리하고, 각 이론이 어떤
> 제약조건/목적함수로 매핑되는지 연결한다.

| 이론/관행 | 설명 | 매핑되는 구현 | 상태 |
|-----------|------|----------------|------|
| *(작성 예정)* | | | ⬜ |

---

## 5. 데이터 모델 (확장 방향) — 🟡

### 5.1 Nurse (간호사)
- `id`, `name`
- `night_eligible: bool` — 신규·임신 시 false (나이트 제외)
- (추후) `experience_level`, `role`(charge 등), `employment_type`(파트타임 등)

### 5.2 WardConfig (병동 설정)
- 요일별 교대별 최소 인원: 평일 / 주말 / 공휴일 구분
- 회전 규칙 매트릭스(6절 참고)
- 연속 근무일 상한 / 연속 나이트 상한
- 간호사별 최소 오프

### 5.3 MonthContext (월 컨텍스트)
- `year`, `month` → 일수·요일·주말·공휴일 자동 산출
- 공휴일 소스: 수동 입력 vs 자동 (⬜ 결정 대기)

### 5.4 Carry-over (전월 이월)
- 간호사별 전월 말일 마지막 근무 시퀀스(예: 마지막 1–3일)
- 용도: 월초 연속근무·연속나이트·경계 회전 제약 계산

### 5.5 PreAssigned (사전 배정)
- `{nurse_id, day, code}` — 연차·공가 등 하드 고정

### 5.6 Requests (개인 신청)
- `{nurse_id, day, type: off|night, priority}` — 소프트 반영
- 월 최대 신청 개수 제한 (⬜ 결정 대기)

---

## 6. 제약조건 상세 — 🟡

### 6.1 하드 제약 (반드시 준수)
1. 각 간호사는 하루에 근무/휴무 중 정확히 하나 배정
2. 요일별·교대별 최소 인원 충족
3. **역회전 금지 매트릭스** (전날→다음날 전이 금지)
4. 연속 근무일 상한 (전월 이월 포함)
5. 연속 나이트 상한 (전월 이월 포함)
6. 신규·임신 간호사 나이트 배정 금지
7. 사전 휴무(연차 등) 고정
8. 간호사별 최소 오프
9. *(대원칙에서 추가되는 항목 반영)*

### 6.2 회전 매트릭스 (초안)
근무 순서 랭크: `D(1) → M(2) → E(3) → N(4)`. 연속 근무일에서 랭크가 **감소**하는 전이 =
역회전 → 금지 또는 감점. 나이트 이후는 오프/나이트만 허용.

| 전날↓ \ 다음날→ | D | M | E | N | O |
|---|---|---|---|---|---|
| **D** | 허용 | 허용 | 허용 | 허용 | 허용 |
| **M** | 금지 | 허용 | 허용 | 허용 | 허용 |
| **E** | 금지 | 금지 | 허용 | 허용 | 허용 |
| **N** | 금지 | 금지 | 금지 | 허용 | 허용 |
| **O** | 허용 | 허용 | 허용 | 허용 | 허용 |

> 위 "금지/허용" 경계는 병동 상황·대원칙에 맞춰 조정(일부는 금지 대신 감점 가능).

### 6.3 소프트 제약 (목적함수로 최적화)
- 개인 오프/나이트 신청 반영률 최대화
- 근무일·오프·나이트·**주말 오프** 균등 분배(편차 최소화)
- 순방향 회전 선호 / 비선호 전이 감점

---

## 7. 아키텍처

현재 스택 유지 · 모듈 정리:

```
duty/
├── app/
│   ├── models.py       # 데이터 모델 (확장)
│   ├── config.py       # 기본값·회전 매트릭스 (신규)
│   ├── scheduler.py    # CP-SAT 솔버 (제약별 정리)
│   ├── main.py         # FastAPI
│   └── static/index.html
├── tests/
├── docs/PLAN.md        # 본 문서
└── requirements.txt
```

---

## 8. 단계별 로드맵

| 단계 | 내용 | 산출물 | 상태 |
|------|------|--------|------|
| Phase 0 | 뼈대(현재): 기본 3교대 솔버·API·UI·테스트 | 완료 | ✅ |
| Phase 1 | 데이터 모델 확장 (미드·비근무코드·간호사속성·월컨텍스트·이월·사전배정·신청) | `models.py` + 테스트 | ⬜ |
| Phase 2 | 솔버 고도화 (미드·요일별인원·회전매트릭스·나이트제외·이월계산·공정성 확장) | `scheduler.py` + 테스트 | ⬜ |
| Phase 3 | 웹 UI 고도화 (속성 편집·달력형 그리드·신청/휴무 입력·통계 패널) | 프론트 | ⬜ |
| Phase 4 | (후순위) 수동수정 후 재최적화·프리셋 저장/불러오기·엑셀 내보내기·위반 리포트 | 운영 편의 | ⬜ |

---

## 9. 결정 대기 항목 (Open Questions)

- [ ] 대원칙(0절) 확정
- [ ] 미드(M) 근무 시간대·최소 인원
- [ ] 신규 기준(입사 몇 개월까지 나이트 제외)
- [ ] 개인 신청 월 최대 개수·우선순위 규칙
- [ ] 공휴일 입력 방식(수동 vs 자동)
- [ ] 회전 매트릭스의 "금지 vs 감점" 경계
- [ ] 주말 오프 공정성 정책

---

## 10. 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-07-21 | 최초 작성. 요구사항 인터뷰 결과·로드맵·제약조건 초안 정리. 대원칙/병동상황/이론 섹션 자리 확보. |
